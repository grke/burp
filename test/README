IMPORTANT: Run the script 'run_test' whilst standing in the same directory as
the script.

It will copy the source from the parent directory into 'build', and then
install it into 'target'.

It will then run through some basic tests.
