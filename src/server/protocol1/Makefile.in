@MCOMMON@

srcdir =	.
VPATH = 	.
.PATH:		.

# one up
basedir = ..
# top dir
topdir = ../../..
# this dir relative to top dir
thisdir = src/server/protocol1

#
SRCS = \
	backup_phase2.c \
	backup_phase4.c \
	bedup.c \
	blocklen.c \
	deleteme.c \
	dpth.c \
	fdirs.c \
	link.c \
	restore.c \
	zlibio.c \

vss_strip: Makefile vss_strip.o
	@echo "Linking $@ ..."
	$(LIBTOOL_LINK) $(CXX) $(WLDFLAGS) $(LDFLAGS) -o $@ vss_strip.o

OBJS = $(SRCS:.c=.o)

all: Makefile $(OBJS) vss_strip

@MCOMMON2@
